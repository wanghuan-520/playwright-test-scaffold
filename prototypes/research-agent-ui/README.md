# 🔬 AI 研究助手 - 前端原型

> 基于 [aevatarAI/aevatar-agent-framework#67](https://github.com/aevatarAI/aevatar-agent-framework/issues/67) 需求的快速原型

## 技术栈

- **框架**: Next.js 14 (App Router)
- **样式**: Tailwind CSS
- **状态管理**: Zustand
- **图标**: Lucide React
- **主题**: 暗色主题

## 快速开始

```bash
# 进入目录
cd prototypes/research-agent-ui

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 浏览器打开
open http://localhost:3000
```

## 项目结构

```
research-agent-ui/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 全局布局
│   ├── page.tsx           # 主页面
│   └── globals.css        # 全局样式
├── components/
│   ├── ui/                # 基础 UI 组件
│   │   ├── button.tsx
│   │   └── card.tsx
│   └── stages/            # 阶段组件
│       ├── InputStage.tsx      # 输入方向
│       ├── BriefingStage.tsx   # 研究简报
│       ├── RunningStage.tsx    # 并行调研
│       ├── ComputeStage.tsx    # 运算确认
│       ├── MonitoringStage.tsx # 运算监控
│       └── DeliveryStage.tsx   # 交付中心
├── lib/
│   ├── store.ts           # Zustand 状态管理
│   ├── mock-data.ts       # Mock 数据生成
│   └── utils.ts           # 工具函数
├── types/
│   └── index.ts           # TypeScript 类型定义
└── hooks/                 # 自定义 Hooks
```

## 功能演示

### 1. 输入研究方向
- 输入研究方向
- 设置边界约束（预算/速度/严谨度）
- 点击"开始研究"

### 2. 确认研究简报
- 查看系统理解的问题
- 确认/修改假设
- 查看风险和里程碑
- 点击"继续"或"纠偏"

### 3. 查看调研进度
- 实时 Agent 状态
- 实时日志
- 当前发现

### 4. 运算确认
- 查看执行计划
- 选择：执行/降级/跳过

### 5. 运算监控
- 状态时间线
- 进度条
- 中间结果快照

### 6. 交付物中心
- 论文草稿
- 结论清单
- 下一轮建议

## Mock 数据说明

当前使用 Mock 数据模拟后端响应：
- `lib/mock-data.ts` 生成模拟数据
- `lib/store.ts` 中的 `simulateProgress` 模拟状态变化

## 后续开发

接入真实后端需要：

1. **替换 Mock 数据**
   - 创建 API 客户端
   - 替换 store 中的 action

2. **添加 WebSocket**
   - 实时接收 Agent 状态
   - 实时接收日志

3. **添加错误处理**
   - 网络错误
   - 超时处理

4. **添加持久化**
   - 本地存储研究历史
   - 支持断点续传

## License

MIT

