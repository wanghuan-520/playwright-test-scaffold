"""
Research Agent 测试配置
集成 Eval 框架，提供 AI 输出质量评估
"""

import pytest

# 加载 Eval pytest 插件
pytest_plugins = ["eval.pytest_plugin"]


# ============================================================
# 模拟 AI 输出 Fixtures
# ============================================================

@pytest.fixture
def mock_research_briefing() -> str:
    """模拟：研究简报输出"""
    return """
# 研究简报：多 Agent 协作架构在 AI 科研中的应用探索

## 摘要

本研究简报旨在探索多 Agent 协作架构在 AI 辅助科研中的应用潜力。研究表明，通过合理的角色分工和
共识机制，多 Agent 系统可以显著提升研究效率和输出质量。根据初步调研数据，采用多 Agent 架构的
研究团队在文献综述效率上提升了 50%，在结论可靠性上提升了 30%。

## 问题改写

用户希望探索多 Agent 协作在 AI 科研中的应用，重点关注以下三个核心问题：
1. 如何通过角色分工提升研究效率
2. 如何通过共识机制保证结论质量
3. 如何平衡自动化程度与人工控制

## 引言

大语言模型（LLM）的发展为科研自动化提供了新的可能性。然而，单一 Agent 在处理复杂研究任务时
存在明显局限性。本研究提出的多 Agent 协作架构，通过模拟人类科研团队的分工模式，有效解决了
这些问题。实验数据表明，多 Agent 系统在复杂任务中的表现优于单一 Agent。

## 假设清单

基于文献调研和初步实验，我们提出以下核心假设：
1. **效率假设**：多 Agent 分工可以通过并行处理提升研究效率，预期提升 40-60%
2. **质量假设**：共识机制可以通过多视角验证保证结论质量，预期准确率提升 25%
3. **控制假设**：层级确认策略可以在保持自动化的同时给予用户必要的控制权

## 风险清单

根据相关研究和技术分析，我们识别了以下潜在风险：
- **高风险**：Agent 之间的协调开销可能抵消效率收益，需要优化通信协议
- **中风险**：共识机制可能导致保守结论，需要引入多样性激励机制
- **低风险**：用户学习成本较高，需要提供直观的交互界面

## 里程碑与计划

1. **文献调研**（2 天）：收集多 Agent 协作、AI 科研相关文献 50 篇以上
2. **架构设计**（3 天）：设计 6 种专业化 Agent 的角色和协作机制
3. **原型实现**（5 天）：实现核心功能的原型系统
4. **评估验证**（3 天）：在 20 个真实研究任务上进行评估

## 结论

综上所述，多 Agent 协作架构为 AI 科研助手提供了一条可行的技术路径。通过合理的角色分工、
共识机制和风险控制，可以在提升效率的同时保证研究质量。
"""


@pytest.fixture
def mock_conclusion_card() -> str:
    """模拟：结论卡输出"""
    return """
# 结论卡 #001

## 结论
多 Agent 协作架构相比单 Agent 方案，在复杂研究任务中效率提升 45%，准确率提升 20%。

## 可信度
**高**（基于 50 个真实案例的实验验证）

## 证据链接
1. [实验报告 A] 对照实验显示多 Agent 完成时间减少 60%
2. [文献 B] 类似架构在代码生成任务中达到 92% 准确率
3. [数据集 C] 1000 条推理任务的评估结果

## 局限性
- 仅在研究类任务中验证
- 需要较高的计算资源
"""


@pytest.fixture
def mock_paper_draft() -> str:
    """模拟：论文草稿输出"""
    return """
# 基于多 Agent 协作的 AI 科研助手系统研究

## 摘要

本研究提出了一种基于多 Agent 协作的 AI 科研助手架构。通过将研究任务分解给 6 种专业化 Agent
（Planner、Librarian、Reasoner、Verifier、Compute、Deliverables），实现了高效的科研自动化。
在 50 个真实研究任务上的实验表明，该系统相比单 Agent 方案，研究完成时间减少 60%，结论准确率
达到 92%，用户干预次数减少 80%。

## 1. 引言

大语言模型（LLM）的快速发展为科研自动化提供了新的可能性。然而，单一 Agent 在处理复杂研究任务
时存在明显局限性：上下文长度限制、推理深度不足、缺乏专业知识整合等。本研究提出的多 Agent 协作
架构，通过模拟人类科研团队的分工模式，有效解决了这些问题。

根据 2024 年的调研数据，使用 AI 辅助的研究团队论文发表效率平均提升 30%，但质量控制仍是主要挑战。
我们的系统通过引入 Verifier 角色和共识机制，在保持效率的同时确保了输出质量。

## 2. 方法

### 2.1 架构设计

系统采用分层架构：
- **协调层**：研究总控（RA）负责接收用户输入，调度各 Agent
- **执行层**：6 种专业 Agent 并行工作
- **验证层**：Verifier 复核所有结论

### 2.2 共识机制

不同于传统系统需要人工审批每一步，我们采用基于风险的分层确认策略：
- 低风险操作：自动执行，事后通知
- 高风险操作：生成「执行计划卡」，用户确认后执行

## 3. 实验

### 3.1 实验设置
- 数据集：50 个真实研究任务（涵盖文献综述、实验设计、数据分析）
- 对照组：单 Agent（GPT-4）、人工研究
- 评估指标：完成时间、准确率、用户满意度

### 3.2 结果
| 方法 | 完成时间 | 准确率 | 用户满意度 |
|-----|---------|--------|-----------|
| 人工 | 100% | 85% | 70% |
| 单 Agent | 60% | 75% | 65% |
| 多 Agent（我们） | 40% | 92% | 90% |

## 4. 结论

多 Agent 协作架构为 AI 科研助手提供了一种可行且高效的实现路径。实验结果验证了分工协作和共识
机制的有效性。未来工作将探索更多 Agent 角色和更复杂的协作模式。
"""


@pytest.fixture
def mock_low_quality_output() -> str:
    """模拟：低质量输出（用于测试失败场景）"""
    return """
# 结论

AI 很好。多 Agent 也很好。效率提升了。
"""


# ============================================================
# 测试配置
# ============================================================

@pytest.fixture
def research_eval_context() -> dict:
    """研究系统专用评估上下文"""
    return {
        "research_direction": "多 Agent 协作架构",
        "expected_sections": ["摘要", "引言", "方法", "实验", "结论"],
        "keywords": ["Agent", "LLM", "协作", "效率", "准确率"],
        "threshold": 0.7,
    }

