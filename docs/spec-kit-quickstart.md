# Spec-Kit 实战使用指南

## 🚀 快速开始：从想法到测试代码

### 场景：新功能 - "用户个人设置"

假设你要为应用添加一个"用户个人设置"功能，让我们走一遍完整流程。

---

## 📋 完整工作流（5 步）

### 1️⃣ 生成功能规约

**使用命令**：`/speckit.specify`

**操作**：
```
在 Cursor 中输入斜杠命令：
/speckit.specify

然后输入功能描述：
我想添加一个用户个人设置页面，用户可以：
- 修改显示名称
- 更改头像
- 设置语言偏好（中文/英文）
- 配置通知选项（邮件/站内信）
```

**输出**：
- 创建 `specs/015-user-settings/spec.md`
- 包含用户故事、验收标准、边缘情况
- 按优先级（P1/P2/P3）组织功能

**生成的文件示例**：
```markdown
# 功能规约：用户个人设置

## 用户场景和测试

### 用户故事 1 - 修改显示名称（优先级：P1）
用户可以在个人设置页面修改自己的显示名称

**验收场景**：
1. **假设** 用户已登录，**当** 用户访问设置页面并修改显示名称，**那么** 新名称立即生效
2. **假设** 用户输入空名称，**当** 点击保存，**那么** 显示错误提示

### 用户故事 2 - 更改头像（优先级：P2）
...
```

---

### 2️⃣ 生成技术计划（可选，但推荐）

**使用命令**：`/speckit.plan`

**操作**：
```
/speckit.plan

上下文：基于刚生成的 specs/015-user-settings/spec.md
```

**输出**：
- 创建 `specs/015-user-settings/plan.md`
- 技术栈决策（前端/后端/API）
- 数据模型设计
- 项目结构规划

**生成的文件示例**：
```markdown
# 实现计划：用户个人设置

## 技术上下文
**语言/版本**：Python 3.11 + Playwright
**主要依赖关系**：pytest, allure
**目标平台**：Web 应用测试
**项目类型**：测试自动化

## 项目结构
specs/015-user-settings/
├── plan.md              # 此文件
├── spec.md              # 功能规约
└── tasks.md             # 任务列表（下一步生成）

pages/
└── user_settings_page.py    # 页面对象

tests/
└── User/
    └── Settings/
        ├── test_display_name.py
        └── test_avatar.py
```

---

### 3️⃣ 生成任务列表

**使用命令**：`/speckit.tasks`

**操作**：
```
/speckit.tasks

基于 specs/015-user-settings/ 的规约和计划
```

**输出**：
- 创建 `specs/015-user-settings/tasks.md`
- 可执行的任务清单（带复选框）
- 按用户故事分组
- 标记并行任务 [P]
- 包含文件路径

**生成的文件示例**：
```markdown
# 任务：用户个人设置

## 第 1 阶段：设置
- [ ] T001 创建 pages/user_settings_page.py
- [ ] T002 [P] 在 test-data/ 中准备测试数据

## 第 2 阶段：基础
- [ ] T003 实现 user_settings_page.py 的定位器
- [ ] T004 实现基础页面动作（navigate, fill, save）

## 第 3 阶段：用户故事 1 - 修改显示名称（P1）🎯 MVP
- [ ] T005 [US1] 在 tests/User/Settings/test_display_name.py 实现 P1 测试
- [ ] T006 [US1] 实现成功修改场景
- [ ] T007 [US1] 实现验证失败场景

## 第 4 阶段：用户故事 2 - 更改头像（P2）
- [ ] T008 [US2] 实现头像上传测试
...
```

---

### 4️⃣ 执行实现（AI 辅助）

**使用命令**：`/speckit.implement`

**操作**：
```
/speckit.implement

按照 specs/015-user-settings/tasks.md 的任务列表执行
```

**AI 会做什么**：
1. 读取 tasks.md 中的任务
2. 按阶段顺序执行：
   - Phase 1: 创建 Page Object 文件
   - Phase 2: 实现定位器和基础方法
   - Phase 3: 实现测试用例（按用户故事）
3. 每完成一个任务，在 tasks.md 中标记为 `[x]`
4. 提供进度报告

**生成的代码示例**：

`pages/user_settings_page.py`:
```python
from core.base_page import BasePage
import allure

class UserSettingsPage(BasePage):
    def __init__(self, page):
        super().__init__(page)
        self.display_name_input = "#displayName"
        self.save_button = "button[type='submit']"
        self.success_message = ".alert-success"
    
    @allure.step("修改显示名称为 {display_name}")
    def change_display_name(self, display_name: str):
        self.page.fill(self.display_name_input, display_name)
        self.page.click(self.save_button)
        self.wait_for_success()
```

`tests/User/Settings/test_display_name.py`:
```python
import allure
import pytest

@allure.feature("用户个人设置")
@allure.story("修改显示名称")
@pytest.mark.p1
def test_change_display_name_success(user_settings_page, test_account):
    """P1: 用户成功修改显示名称"""
    with allure.step("导航到设置页面"):
        user_settings_page.navigate()
    
    with allure.step("修改显示名称"):
        new_name = "新的显示名称"
        user_settings_page.change_display_name(new_name)
    
    with allure.step("验证名称已更新"):
        assert user_settings_page.get_display_name() == new_name
```

---

### 5️⃣ 质量验证（可选）

**使用命令**：`/speckit.analyze`

**操作**：
```
/speckit.analyze

分析 specs/015-user-settings/ 的文档一致性
```

**输出**：
- 检查 spec.md、plan.md、tasks.md 之间的一致性
- 识别遗漏的需求
- 发现重复或冲突
- 提供修复建议

---

## 🎯 常用场景速查

### 场景 1：快速生成新功能测试

```bash
# 一条龙流程
/speckit.specify → 描述功能
/speckit.tasks   → 生成任务（跳过 plan 也可以）
/speckit.implement → AI 自动实现
```

### 场景 2：完善现有规约

```bash
# 已有 spec.md，需要澄清模糊点
/speckit.clarify → AI 会问 5 个针对性问题
                 → 答案直接写回 spec.md
```

### 场景 3：生成质量检查清单

```bash
/speckit.checklist → 输入检查清单类型（如 "安全"、"UX"、"API"）
                   → 生成需求质量验证清单
```

### 场景 4：手动实现时参考

```bash
# 只生成任务清单，自己手动实现
/speckit.specify
/speckit.tasks
# 然后参考 tasks.md 手动写代码
```

---

## 💡 实战技巧

### 技巧 1：增量开发（推荐）

```bash
# 只实现 MVP（用户故事 1）
/speckit.implement → 只完成 Phase 1-3（User Story 1）
                   → 测试验证
                   → 再继续 Phase 4（User Story 2）
```

### 技巧 2：并行开发

如果团队有多人：
```bash
# 基础完成后，不同人负责不同用户故事
开发者 A: Phase 3（User Story 1）
开发者 B: Phase 4（User Story 2）
开发者 C: Phase 5（User Story 3）
```

### 技巧 3：使用宪法检查

在生成 plan.md 时，AI 会自动检查是否符合宪法：
- 文件是否超过 800 行？
- 是否有 > 3 层缩进？
- 是否符合安全约束？

### 技巧 4：证据链验证

生成的测试代码会自动包含：
```python
with allure.step("关键步骤"):
    # 操作
    self.page.screenshot(path="evidence.png")  # 自动截图
```

---

## 🔧 实际操作演示

### 立即尝试：改进现有功能

你的项目已经有 `specs/002-admin_profile/`，让我们改进它：

```bash
# 1. 检查现有规约的质量
/speckit.analyze
# → AI 会指出 spec.md、plan.md、tasks.md 的不一致

# 2. 澄清模糊点
/speckit.clarify
# → AI 问："头像上传的文件大小限制是多少？"
# → 你答："最大 2MB"
# → AI 自动更新 spec.md

# 3. 重新生成任务（如果需要）
/speckit.tasks
# → 基于更新的 spec.md 生成更精确的任务

# 4. 生成质量检查清单
/speckit.checklist → 输入 "安全检查清单"
# → 生成 specs/002-admin_profile/checklists/security.md
```

---

## 📚 命令速查表

| 命令 | 用途 | 输入 | 输出 |
|------|------|------|------|
| `/speckit.specify` | 生成功能规约 | 功能描述 | spec.md |
| `/speckit.plan` | 生成技术计划 | spec.md | plan.md |
| `/speckit.tasks` | 生成任务列表 | spec.md + plan.md | tasks.md |
| `/speckit.implement` | 执行任务实现 | tasks.md | 代码文件 |
| `/speckit.analyze` | 一致性分析 | 所有文档 | 分析报告 |
| `/speckit.clarify` | 澄清模糊点 | spec.md | 更新的 spec.md |
| `/speckit.checklist` | 生成检查清单 | 检查清单类型 | checklist.md |
| `/speckit.constitution` | 管理宪法 | 原则更新 | constitution.md |
| `/speckit.taskstoissues` | 转 GitHub Issues | tasks.md | GitHub Issues |

---

## 🎓 学习路径

### 初学者：只用 3 个命令
1. `/speckit.specify` - 描述功能
2. `/speckit.tasks` - 生成任务
3. 手动实现代码（参考任务清单）

### 中级：完整工作流
1. `/speckit.specify` - 生成规约
2. `/speckit.clarify` - 澄清模糊点
3. `/speckit.plan` - 生成计划
4. `/speckit.tasks` - 生成任务
5. 手动/半自动实现

### 高级：全自动化
1. `/speckit.specify` - 生成规约
2. `/speckit.clarify` - 澄清模糊点
3. `/speckit.plan` - 生成计划
4. `/speckit.tasks` - 生成任务
5. `/speckit.implement` - AI 自动实现
6. `/speckit.analyze` - 验证质量
7. `/speckit.checklist` - 生成检查清单

---

## ⚠️ 注意事项

### 宪法约束（自动检查）
- ✅ 每个文件 ≤ 800 行
- ✅ 每个文件夹 ≤ 8 个文件
- ✅ 函数 ≤ 20 行
- ✅ 缩进 ≤ 3 层
- ✅ 关键步骤有截图证据

### 测试数据隔离
- 使用 `test-data/test_account_pool.json`
- 基线-修改-回滚模式
- 不污染共享数据

### 安全测试（P1）
- XSS 载荷不能执行
- SQLi 载荷不能导致 5xx
- 敏感数据不能泄露

---

## 🚀 下一步

### 立即开始
1. 选择一个现有功能或新功能
2. 运行 `/speckit.specify` 描述它
3. 查看生成的 `specs/###-feature/spec.md`
4. 继续运行其他命令完善

### 进阶实践
- 尝试用 `/speckit.implement` 让 AI 自动生成代码
- 用 `/speckit.checklist` 生成不同类型的检查清单
- 用 `/speckit.analyze` 验证现有文档的质量

---

**祝你使用愉快！记住：规约驱动开发 = 先思考（WHAT）→ 再计划（HOW）→ 最后编码（CODE）**

