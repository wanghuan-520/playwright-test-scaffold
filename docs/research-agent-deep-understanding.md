# 🧠 AI 研究助手系统 - 深层理解

> 基于 [aevatarAI/aevatar-agent-framework#67](https://github.com/aevatarAI/aevatar-agent-framework/issues/67) 的深度解读

---

## 核心理念

**用户只给方向，系统自主研究，只在关键节点确认**

就像你雇了一个博士生团队帮你做研究，你只需要：
1. 告诉他们研究什么
2. 在关键决策点拍板
3. 最后收论文

---

## 1️⃣ 验证优先：论文作为可推理的结构化输入

### 核心洞察

论文不是一坨文字，是**可执行的推理图**

```
传统理解：论文 = 线性文档
────────────────────────────────────────────────────────
Abstract → Intro → Method → Results → Conclusion
（一条线，从头读到尾）

                    ↓ 本质理解 ↓

论文 = DAG（有向无环图）= 可执行的推理程序
────────────────────────────────────────────────────────

      ┌─────────┐
      │ 公理 A  │
      └────┬────┘
           │
      ┌────▼────┐     ┌─────────┐
      │ 引理 1  │◄────│ 公理 B  │
      └────┬────┘     └─────────┘
           │
 ┌─────────┼─────────┐
 │         │         │
 ▼         ▼         ▼
┌───┐   ┌───┐   ┌───────┐
│定理│   │推论│   │Section│   ← 每个 Section = 一个 Function
│ 1 │   │ 1 │   │  3.2  │
└─┬─┘   └─┬─┘   └───┬───┘
 │       │         │
 └───────┴────┬────┘
              │
         ┌────▼────┐
         │ 主结论  │   ← 组装多个模块后的新结论
         └─────────┘
```

### 验证流程示例

```python
# 输入：一篇论文（MD 字符串）
paper_md = """
# Section 1: 量子涨落基础
定义：量子涨落 Δx·Δp ≥ ħ/2
...

# Section 2: 从涨落到粒子
推导：当分辨率 m=1 时，tick 机制启动...
依赖：Section 1

# Section 3: 高能物理映射
结论：通过 CAP 支变分原理，可导出...
依赖：Section 1, Section 2
"""

# AI 系统自动：
# 1. 解析依赖关系 → 构建 DAG
# 2. 拓扑排序 → 确定推理顺序
# 3. 逐节点验证 → 检查推理链
# 4. 输出：验证报告 + 知识图谱
```

---

## 2️⃣ 模块化 = Section 即 Function

### 核心洞察

写论文 = 写代码

```
代码世界                        论文世界
───────────────────────────────────────────────────────
function add(a, b)              Section 2.1: 加法定理
  return a + b                    给定 a, b，则 a+b = ...
end

function multiply(x, y)         Section 2.2: 乘法定理
  result = 0                      基于 2.1，推导...
  for i in 1..y:
    result = add(result, x)     # 调用 add
  return result
end

function main()                 Section 5: 主结论
  a = multiply(3, 4)              综合 2.1, 2.2, 3.1...
  b = add(a, 5)                   得出最终结论
  return b
end
```

### 论文的 DAG 结构示例

从量子涨落到高能物理（用户的物理论文）：

```
                  ┌──────────────┐
                  │  tick 机制   │
                  │  (基础公理)  │
                  └──────┬───────┘
                         │
            ┌────────────┼────────────┐
            │            │            │
            ▼            ▼            ▼
      ┌─────────┐  ┌─────────┐  ┌─────────┐
      │  m = 1  │  │  m = 2  │  │  m = 3  │
      │ 量子涨落│  │   ...   │  │   ...   │
      └────┬────┘  └────┬────┘  └────┬────┘
           │            │            │
           └────────────┼────────────┘
                        │
                  ┌─────▼─────┐
                  │ CAP 变分  │
                  │   原理    │
                  └─────┬─────┘
                        │
         ┌──────────────┼──────────────┐
         │              │              │
         ▼              ▼              ▼
    ┌────────┐    ┌────────┐    ┌────────┐
    │ 物理量 │    │ 物理规则│    │ 频率   │
    │ (导出) │    │ (导出) │    │(导出量)│
    └────────┘    └────────┘    └────────┘
```

### DAG 规则

- 整个推理过程是一个有向无环图（DAG）
- 不连着的图（森林）也可以推理
- **有环的本质是等价类，相当于一个节点**

---

## 3️⃣ 总线架构：单入口 + 协调者模式

### 核心洞察

用户只跟一个 AI 聊天 = CPU 只有一个中断入口

```
                   【总线架构】

                   ┌─────────┐
                   │  用户   │
                   │(键盘/鼠标)│
                   └────┬────┘
                        │ 中断
                        ▼
════════════════════════════════════════════════════════
                   【总线 BUS】
                  AI Assistant
                 （唯一入口）
════════════════════════════════════════════════════════
         │          │          │          │
         ▼          ▼          ▼          ▼
    ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
    │Planner │ │Librarian│ │Reasoner│ │Verifier│
    └────────┘ └────────┘ └────────┘ └────────┘

用户不需要知道后面有多少 agents
只需要跟 AI Assistant 聊天
协调、调度、共识 —— 都是 Assistant 的事
```

### 类比 CPU 中断机制

| CPU 中断模型 | AI 研究助手模型 |
|-------------|----------------|
| 键盘中断 → CPU | 用户输入 → AI Assistant |
| CPU 调度进程 | Assistant 调度 Agents |
| 进程间通信（IPC） | Agents 间消息传递 |
| 中断返回结果 | Assistant 返回总结 |

用户不需要知道：
- 哪个 Agent 在工作
- 推理如何进行
- 共识如何达成

---

## 4️⃣ 无需审批：共识即真理

### 核心洞察

多 Agents 共识的结果，不需要人来审批

```
传统审批流程（错误模式）
───────────────────────────────────────────────────────
Agent 推理 → 人审批 → Agent 继续 → 人审批 → ...
                ↑                    ↑
              卡住                  卡住

问题：
1. 人成为瓶颈
2. 人不一定比 agents 共识更正确
3. 打断了推理流
```

```
正确模式：共识即真理
───────────────────────────────────────────────────────

   ┌─────────┐
   │ Agent A │──┐
   │ 结论：X │  │
   └─────────┘  │
                │    ┌─────────────┐
   ┌─────────┐  ├───►│   共识层    │
   │ Agent B │──┤    │ (自动对齐)  │
   │ 结论：X │  │    └──────┬──────┘
   └─────────┘  │           │
                │           ▼
   ┌─────────┐  │    ┌─────────────┐
   │ Agent C │──┘    │ 共识结论：X │  ← 多 Agent 同意
   │ 结论：X │       │ （无需人审批）│
   └─────────┘       └─────────────┘

规则：
- 共识了 → 自动进入知识库
- 没共识 → 标记为「待验证」，继续推理
- 用户不想要这个方向 → 放着就好，不是错的
```

### 错误纠错机制（不是审批）

```
纠错 ≠ 审批
───────────────────────────────────────────────────────
审批：人来判断对错 → 瓶颈
纠错：系统自动发现矛盾 → 自动修正

例子：
┌─────────────────────────────────────────────────────┐
│ Round 1: 结论 A「Prompt 提升 15%」                   │
│ Round 2: 发现新证据，与 A 矛盾                       │
│ Round 3: 自动修正 → 结论 A' 「Prompt 提升 10-20%」   │
│                                                      │
│ 用户看到：结论从「15%」变成「10-20%」               │
│ 用户不需要做任何事，系统自动纠错了                   │
└─────────────────────────────────────────────────────┘
```

---

## 5️⃣ 海量知识库：放着就好，不是错的

### 核心洞察

推理结果 = 知识资产，即使方向不是用户想要的

```
知识库积累模型
───────────────────────────────────────────────────────
用户给方向 A → 系统推理 → 产出知识 K1, K2, K3

┌─────────────────────────────────────────────────────┐
│                     知识库                           │
├─────────────────────────────────────────────────────┤
│  K1: Prompt Engineering 提升 15%     ✅ 用户想要     │
│  K2: ReAct vs CoT 对比               ✅ 用户想要     │
│  K3: 多模态代码生成可行性            ❓ 用户没要求   │
│      ↑                                               │
│      │                                               │
│      └── 放着就好！                                   │
│          - 不是错的                                   │
│          - 将来可能有用                               │
│          - 建立了知识关联                             │
└─────────────────────────────────────────────────────┘

本质：每次推理都在扩展知识图谱
      即使当前任务不需要，也是资产
```

---

## 📊 完整架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        【AI 研究助手系统架构】                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   ┌───────────┐                                                      │
│   │   用户    │ ─── 只给方向，不审批，不干预推理                     │
│   └─────┬─────┘                                                      │
│         │                                                            │
│         ▼                                                            │
│   ╔═══════════════════════════════════════════════════════════╗     │
│   ║              AI Assistant（总线/单入口）                   ║     │
│   ║  - 接收用户输入（中断）                                    ║     │
│   ║  - 协调所有 Agents                                         ║     │
│   ║  - 汇总共识结果                                            ║     │
│   ║  - 只在「烧钱」时问一句                                    ║     │
│   ╚═══════════════════════════════════════════════════════════╝     │
│         │                                                            │
│         ├─────────────┬─────────────┬─────────────┐                 │
│         ▼             ▼             ▼             ▼                 │
│   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐            │
│   │ Planner │   │Librarian│   │ Reasoner│   │ Verifier│            │
│   │ DAG构建 │   │ 证据收集│   │ 推理引擎│   │ 共识复核│            │
│   └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘            │
│        │             │             │             │                   │
│        └─────────────┴──────┬──────┴─────────────┘                  │
│                             │                                        │
│                             ▼                                        │
│   ╔═══════════════════════════════════════════════════════════╗     │
│   ║                      共识层                                ║     │
│   ║  - 多 Agent 结论对齐                                       ║     │
│   ║  - 冲突检测 & 自动纠错                                     ║     │
│   ║  - 无需人审批                                              ║     │
│   ╚═══════════════════════════════════════════════════════════╝     │
│                             │                                        │
│                             ▼                                        │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                    知识库（海量积累）                        │   │
│   │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │   │
│   │  │ 结论卡  │  │ 证据链  │  │ 推理图  │  │ 论文稿  │        │   │
│   │  │  清单   │  │  清单   │  │  (DAG)  │  │ (可读)  │        │   │
│   │  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │   │
│   │                                                              │   │
│   │  放着就好，不是错的 → 将来可能有用                          │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📋 用户体验总结表

| 阶段 | 用户看到什么 | 用户需要做什么 | 设计目标 |
|------|-------------|---------------|---------|
| **输入方向后** | 研究简报 + 假设 + 路线图 | 继续或纠偏 | 防跑偏 |
| **并行调研中** | 进度面板 + Agent 状态 | 可忽略 | 有掌控感但不打扰 |
| **出现关键结论** | 结论卡 + 证据对照 | 可选点评 | 快速判断优先级 |
| **需要程序运算** | 执行计划卡 + 三选一 | 一键选择 | **只在必要时打扰** |
| **运算执行中** | 状态时间线 + 中间快照 | 默认不操作 | 长任务不黑箱 |
| **运算完成** | 结果摘要 + 对结论影响 | 可选指定偏好 | 把计算变成证据 |
| **每轮结束** | 本轮总结 + 下一轮选项 | 选方向或自动继续 | 持续迭代节拍 |

---

## 🎯 一句话总结

> **论文 = DAG，Section = Function，推理 = 拓扑排序执行**
> **用户 = 键盘输入，AI Assistant = CPU 中断处理**
> **共识 = 真理，不需要审批**
> **知识库 = 资产，放着就好**

这不是一个「问答系统」，而是一个**可持续积累的 AI 科研院**。

---

## 📚 相关资源

- [GitHub Issue #67](https://github.com/aevatarAI/aevatar-agent-framework/issues/67) - 原始需求文档
- [测试计划](./test-plans/research-agent-system.md) - 详细测试用例
- [原型图](./test-plans/research-agent-system-prototype.md) - ASCII 原型设计
- [前端原型](../prototypes/research-agent-ui/) - 可运行的 UI 原型

