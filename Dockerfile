# ═══════════════════════════════════════════════════════════════
# Playwright Test Scaffold - Docker Image
# ═══════════════════════════════════════════════════════════════
# 构建：docker build -t playwright-test-scaffold .
# 运行：docker run -it playwright-test-scaffold pytest tests/

FROM mcr.microsoft.com/playwright/python:v1.49.0-jammy

# ─────────────────────────────────────────────────────────────────
# 设置工作目录
# ─────────────────────────────────────────────────────────────────
WORKDIR /app

# ─────────────────────────────────────────────────────────────────
# 复制依赖文件并安装
# ─────────────────────────────────────────────────────────────────
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# ─────────────────────────────────────────────────────────────────
# 复制项目文件
# ─────────────────────────────────────────────────────────────────
COPY . .

# ─────────────────────────────────────────────────────────────────
# 创建必要目录
# ─────────────────────────────────────────────────────────────────
RUN mkdir -p screenshots reports allure-results allure-report

# ─────────────────────────────────────────────────────────────────
# 设置环境变量
# ─────────────────────────────────────────────────────────────────
ENV PYTHONPATH=/app
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# 跳过服务检查（容器内运行时服务地址不同）
ENV PRECHECK_SERVICES=0

# ─────────────────────────────────────────────────────────────────
# 默认命令
# ─────────────────────────────────────────────────────────────────
CMD ["pytest", "tests/framework/", "-v", "--tb=short"]

